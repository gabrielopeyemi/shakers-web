import React, { ReactElement } from 'react';
import { Container } from '../main.styles';

interface Props {
    
}

export default function StartPages({}: Props): ReactElement {
    return (
        <Container>
            Start Page
        </Container>
    )
}
